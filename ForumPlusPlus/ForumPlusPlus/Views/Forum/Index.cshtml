@model ForumIndexModel 

<h1>Forums</h1>

<table class="table table-hover" id="forumIdnexTable"> 
    <tbody>
        @foreach(var forum in Model.forumViewModels)
        {
            <tr>
                <td>
                    <a asp-controller="Forum" asp-action="Topic" asp-route-forumId="@forum.Id">
                        @forum.Name
                    </a>
                </td>
                <td>
                    @forum.Description
                </td>
            </tr>
        }
    </tbody>
</table>